language: cpp

compiler:
  - clang
  - gcc

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y cmake llvm-dev

#addons:
#  apt:
#    packages:
#    - cmake
#    - llvm-dev

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - make test
